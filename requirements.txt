# ── Runtime dependencies ──────────────────────────────────────────────────────
streamlit>=1.35.0       # Streamlit dashboard (app.py)
plotly>=5.20.0          # Interactive charts in the dashboard
pandas>=2.2.0           # DataFrames for results table
numpy>=1.26.0           # Numeric ops & synthetic benchmarks

# ── Optional / development ────────────────────────────────────────────────────
psutil>=6.0.0           # System metrics (CPU, memory, I/O) — optional extension
tabulate>=0.9.0         # Pretty CLI tables — optional extension
pytest>=8.0.0           # Unit tests  →  run: pytest tests/
